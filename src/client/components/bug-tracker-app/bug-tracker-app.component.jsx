import React from 'react';

// COMPONENTS
import BugList from '../bug-list/bug-list.component.jsx';

const BugTrackerApp = (props) => {
    return (
        <div>
            <BugList />
        </div>
    );
};

export default BugTrackerApp;